#include <iostream>
using namespace std;
int main()
{
    for(int count= 1; count <= 10; count = count +1 )
    {
        cout<< "I a not Happy!"<<endl;
    }
    // int count = 1 , is variable initialization, it execute only first time
    // count <= 10 , is condition statement
    // count = count +1 is update stateent 
    return 0;
}